# ── NexusRoute AI — Frontend Environment Variables ─────────────────────────────
# Copy this file to .env and set the backend URL if not using default localhost.

REACT_APP_API_URL=http://localhost:5000
